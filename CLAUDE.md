## Development Workflow

1. **Git Workflow**
   - Commit message follow Conventional Commits Specification
     - Use the `/commit` command to generate properly formatted commit messages
   - Pull Request titles must follow the format:
     ```
     <type>(<scope>): <description>
     ```
   - Use the `/create-pr` command to create properly formatted PR